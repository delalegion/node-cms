<div class="uk-container uk-container-small uk-margin-top">

    <% if (errors) { %>
        <div class="uk-alert-danger" uk-alert>
            <a class="uk-alert-close" uk-close></a>
            <p>Wystąpił błąd. Sprawdź wszystkie wprowadzone dane.</p>
        </div>
    <% } %>

    <form method="POST">
        <div class="block uk-margin-top">
            <label>
                <span class="uk-form-label">Imię i nazwisko</span>
                <!-- Using form state modifers, the classes can be identical for every input -->
                <input value="<%= form.name %>" type="text" placeholder="Imię i nazwisko..." name="name" class="uk-input <% if (errors?.name) { %>  uk-form-danger  <% } %>"/>
                <% if (errors?.name) { %>
                    <div class="uk-alert-danger uk-margin-small-top"" uk-alert>
                        <p><%= errors?.name.message %></p>
                    </div>
                <% } %>
            </label>
        </div>
        <div class="block uk-margin-top">
            <label>
                <span class="uk-form-label">Slug</span>
                <!-- Using form state modifers, the classes can be identical for every input -->
                <input value="<%= form.slug %>" type="text" placeholder="Slug..." name="slug" class="uk-input <% if (errors?.slug) { %>  uk-form-danger  <% } %>"/>
                <% if (errors?.slug) { %>
                    <div class="uk-alert-danger uk-margin-small-top"" uk-alert>
                        <p><%= errors?.slug.message %></p>
                    </div>
                <% } %>
            </label>
        </div>
        <div class="block uk-margin-top">
            <label>
                <span class="uk-form-label">Email</span>
                <!-- Using form state modifers, the classes can be identical for every input -->
                <input value="<%= form.email %>" type="email" placeholder="email@gmail.com" name="email" class="uk-input" />
                <% if (errors?.email) { %>
                    <div class="uk-alert-danger uk-margin-small-top" uk-alert>
                        <p><%= errors.email.message %></p>
                    </div>
                <% } %>
            </label>
        </div>
        <button type="submit" class="uk-button uk-button-primary uk-margin-top">
            <span class="material-symbols-outlined">supervisor_account</span>
            Create new user
        </button>
    </form>
</div>